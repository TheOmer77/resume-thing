import type { BlockData } from '@/types/blocks';

export const resumeBlocks = [
  {
    id: 'dummy-title',
    type: 'title',
    content: {
      title: 'Name Lastname',
      subtitle: 'Job Title',
    },
  },
] satisfies BlockData[];
